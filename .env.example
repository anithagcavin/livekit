# Copy to .env and fill in values
OPENAI_API_KEY=
# Default to local SQLite database
DB_URL=sqlite:////workspace/app/data.db
# Optional: override model
OPENAI_MODEL=gpt-4o-mini
# Optional: max rows limit per query
MAX_RESULT_ROWS=200